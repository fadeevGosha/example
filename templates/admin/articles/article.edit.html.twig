{% extends 'base.html.twig' %}

{% block title %} Изменение статьи {% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            {%if showError %}
                <div class="col-12">
                    <div class="alert alert-danger" role="alert">
                        Не удалось изменить статью
                    </div>
                </div>
            {% endif %}
            <div class="col-12">
                <div class="border flex-md-row mb-4 shadow-sm bg-white p-4">
                    <h1>Изменение статьи</h1>
                    {% include 'admin/articles/form.html.twig' with {button_text: 'Изменить'}  %}
                </div>

            </div>

        </div>
    </div>
{% endblock %}

